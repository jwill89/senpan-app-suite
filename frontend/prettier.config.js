// Prettier settings come from the shared @jwill89/eslint-config (single source of
// truth for formatting across jwill89 frontends): semi:false, singleQuote:true,
// printWidth:100, trailingComma:'all'.
export { default } from '@jwill89/eslint-config/prettier'
